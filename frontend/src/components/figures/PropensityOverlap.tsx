// frontend/src/components/figures/PropensityOverlap.tsx
import { FigureImage } from "./Figure"; export default function PropensityOverlap({ url }: { url: string }) { return <FigureImage src={url} alt="Propensity Overlap" />; }

